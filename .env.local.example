# Application Database (for storing challenges, user history, etc.)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/dailysql

# Sandboxed Runner Database (for running arbitrary user queries safely)
# Make sure the user daily_sql_runner only has restricted access to this database
SANDBOX_DATABASE_URL=postgresql://daily_sql_runner:runnerpassword@localhost:5432/dailysql_sandbox
