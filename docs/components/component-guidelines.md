# Component Guidelines

Guidelines for building UI components.

Principles
- Small and focused: one responsibility per component.
- Prefer composition over inheritance.
- Components should be framework-idiomatic (React hooks, props-first).

Files
- Each component lives in a folder with `index.tsx`, `styles.module.css` (or preferred styling), and `README.md` for usage notes.
