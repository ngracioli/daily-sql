# Getting Started

Quick steps to run the app locally.

Prerequisites
- Node.js 18+ and pnpm or npm

Local dev
1. Install dependencies: `pnpm install` (or `npm install`)
2. Run dev server: `pnpm dev` (or `npm run dev`)

Tests & lint
- `pnpm test`
- `pnpm lint`
